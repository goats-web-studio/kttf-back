/**
 * Отправка одноразового кода.
 *
 * Порт, а не конкретный провайдер: владелец продукта решил не подключать SMS
 * на этой стадии (ОВ-9). Контракт ТС 7.1 от этого не меняется — меняется
 * только то, что стоит за этим интерфейсом. Появится договор с Mobizon или
 * SMSC.kz (ТС 2.5) — добавится второй адаптер, эндпоинты не тронутся.
 */
export interface CodeSender {
  send(phone: string, code: string): Promise<void>;
}

/** Токен внедрения: интерфейс — тип, в контейнер его не положить. */
export const CODE_SENDER = 'KTTF_CODE_SENDER';
